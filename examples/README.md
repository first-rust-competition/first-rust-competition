# Examples

`cargo build` will build all of the examples. To test them out, use `cargo-frc`.
Set the `package.metadata.frc.executable-name` key to the test name you would like to try, then run `cargo frc deploy`.
